<%= form_with(model: tags_post) do |form| %>
  <% if tags_post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tags_post.errors.count, "error") %> prohibited this tags_post from being saved:</h2>

      <ul>
        <% tags_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :post_id, style: "display: block" %>
    <%= form.text_field :post_id %>
  </div>

  <div>
    <%= form.label :tag_id, style: "display: block" %>
    <%= form.text_field :tag_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
